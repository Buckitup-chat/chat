{
  "scripts": {
    "deploy": "cd .. && mix assets.deploy && rm -f _build/esbuild"
  },
  "devDependencies": {
    "tailwindcss-font-inter": "^3.0.1"
  },
  "dependencies": {
    "peaks.js": "^2.1.0"
  }
}
