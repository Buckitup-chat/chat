defmodule Chat.GalleryTest do
  use ExUnit.Case

  # todo:
  test "should provide next and prev images in dialog", do: :todo

  # todo:
  test "should provide next and prev inages in room", do: :todo
end
