defmodule Chat.Rooms.PrivateRoomTest do
  use ExUnit.Case, async: true

  test "should create as usual", do: :todo
  test "should not be seen in list", do: :todo
  test "should be joinable by getting a room key message in dialog", do: :todo
  test "should be seen in list when i have a key", do: :todo
  test "invite rooms list should contain only private ones", do: :todo
  test "adding request should do nothing", do: :todo
  test "apprioving requests should do nothing", do: :todo

  test "what else ?", do: :todo
end
